export enum UserRoles {
    super_admin = 'super_admin',
    admin = 'admin',
    staff = 'staff',
    student = 'student',
    pending = 'pending',
}

export enum RoleWeight {
    super_admin,
    admin,
    staff,
    student,
    pending,
}
